---
interface Props {
  title: string;
  desc: string;
  tags: string[];
  delay?: string;
}
const { title, desc, tags, delay = "0" } = Astro.props;
---
<div data-aos="fade-up" data-aos-delay={delay} class="group">
  <div class="aspect-video bg-slate-100 dark:bg-slate-900 rounded-[2.5rem] mb-6 overflow-hidden relative border border-slate-200 dark:border-blue-500/10 transition-all duration-500 shadow-sm group-hover:shadow-2xl group-hover:shadow-blue-500/10 group-hover:-translate-y-2">

    <div class="absolute inset-0 bg-gradient-to-br from-blue-600/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 z-10"></div>

    <div class="w-full h-full flex items-center justify-center font-black text-slate-300 dark:text-slate-700 text-xl uppercase tracking-tighter italic">
      Project Preview
    </div>

    <div class="absolute bottom-4 right-4 z-20 opacity-0 group-hover:opacity-100 transition-all duration-500 translate-y-4 group-hover:translate-y-0">
       <div class="px-4 py-2 bg-white/10 backdrop-blur-md border border-white/20 rounded-full text-[10px] font-bold text-white uppercase tracking-widest">
         View Details
       </div>
    </div>
  </div>

  <div class="flex justify-between items-start px-2">
    <div class="max-w-[80%]">
      <h4 class="text-2xl font-black mb-2 tracking-tight text-slate-900 dark:text-white group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors">
        {title}
      </h4>
      <p class="text-slate-500 dark:text-slate-400 text-sm mb-4 leading-relaxed line-clamp-2">
        {desc}
      </p>

      <div class="flex flex-wrap gap-2">
        {tags.map(tag => (
          <span class="text-[9px] font-bold px-3 py-1 bg-blue-50 dark:bg-blue-900/20 text-blue-600 dark:text-blue-400 border border-blue-100 dark:border-blue-500/20 rounded-lg uppercase tracking-widest transition-colors group-hover:border-blue-500/40">
            {tag}
          </span>
        ))}
      </div>
    </div>

    <a href="#" class="w-12 h-12 bg-white dark:bg-slate-900 rounded-2xl flex items-center justify-center border border-slate-200 dark:border-slate-800 text-slate-900 dark:text-white hover:bg-blue-600 dark:hover:bg-blue-500 hover:text-white hover:border-blue-600 dark:hover:border-blue-500 transition-all duration-300 shadow-lg shadow-transparent hover:shadow-blue-500/20 mt-1">
      <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
        <path stroke-linecap="round" stroke-linejoin="round" d="M14 5l7 7m0 0l-7 7m7-7H3" />
      </svg>
    </a>
  </div>
</div>
